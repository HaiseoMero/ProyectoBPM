# URL base de la API FastAPI. Copia este archivo como ".env" y ajusta el valor
# cuando el backend esté disponible (por ejemplo, tras levantar Docker/Camunda).
VITE_API_BASE_URL=http://localhost:8000/api
